"""Central configuration for TORQ.

Holds environment-driven settings, model/provider selection, feature flags,
and graceful-fallback toggles used across every stage of the pipeline.
"""

# TODO: load env vars (Postgres, Qdrant, MQTT broker, Twilio, MCP endpoints)
# TODO: model/provider config (LLM + embedding model names, API keys)
# TODO: feature flags (enable_whatsapp, require_approval, trilingual_pdf)
# TODO: graceful-fallback toggles (mock_twilio, in_app_only, offline_mode)
