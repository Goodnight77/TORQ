"""FastAPI application entrypoint.

Creates and configures the FastAPI app for the TORQ dashboard and approval
queue, mounting routes and startup/shutdown hooks.
"""

# TODO: create FastAPI app, configure CORS and middleware
# TODO: include routers from api.routes
# TODO: startup/shutdown hooks (DB, MQTT listener, MCP)
# TODO: uvicorn entrypoint for local/demo run
