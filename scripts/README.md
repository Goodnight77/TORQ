# Dev / Demo Scripts

Helper scripts for local development and running the scripted demo.

- **run simulator** — publish machine fault codes over MQTT to trigger the pipeline.
- **seed db** — load demo scenarios and baseline state.
- **ingest manuals** — index OEM manuals for agent retrieval (via MCP).
- **demo runner** — orchestrate the end-to-end fault to fix demo flow.
